---

name: 🇨🇩 Tag Demo

on:
    push:
        branches:
            - 'release/*'

jobs:
    stable:
        name: 🐴 Stable
        runs-on: ubuntu-latest
        steps:
            -
                id: tagger
                run: echo "::set-output image_tag=$(echo ${{github.ref}} | awk -F/ '{print $NF}')"
            -
                id: builder
                run: echo ${{steps.tagger.outputs.image_tag}}

...
# -*- mode: yaml; indent: 4; fill-column: 120; coding: utf-8 -*-
